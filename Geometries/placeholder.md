# src placeholder

This directory is reserved for future **simulation** and **visualization** modules built on top of the geometry defined in `docs/`.

Potential components:

- state–space visualizer for T, C, E, G, P, κ  
- interactive sliders and trajectory animations  
- agent‑based models embedded in the defined axes  
- policy intervention experiments  

For now, this file simply marks the intended purpose of the `src/` tree.
